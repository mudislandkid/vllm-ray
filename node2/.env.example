# Hugging Face token for accessing gated models
HF_TOKEN=your_huggingface_token_here

# Ray head node IP address
RAY_HEAD_IP=10.15.105.105

# Model cache directory (should match node1 if using shared storage)
MODEL_CACHE_DIR=./models

# Network interface for distributed communication (GLOO/NCCL)
# Find your interface with: ip addr show
NETWORK_INTERFACE=ens33

# Critical: This node's IP address (must be set correctly!)
# vLLM and Ray use this to communicate between nodes
# Find with: ip addr show ens33 | grep "inet "
VLLM_HOST_IP=10.15.105.107
