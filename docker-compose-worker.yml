services:
  ray-worker:
    build: .
    image: vllm-ray
    container_name: ray-worker
    entrypoint: /bin/bash
    command: -c "ray start --address=${HEAD_NODE_IP}:6379 --node-ip-address=${WORKER_NODE_IP} --block"
    network_mode: host
    ipc: host
    shm_size: "10.24g"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    volumes:
      - /home/vectir/.cache/huggingface:/root/.cache/huggingface
    environment:
      - VLLM_HOST_IP=${WORKER_NODE_IP}
      - NCCL_SOCKET_IFNAME=${NETWORK_INTERFACE}
      - GLOO_SOCKET_IFNAME=${NETWORK_INTERFACE}
      - NCCL_DEBUG=WARN
      - HF_TOKEN=${HF_TOKEN}
    restart: unless-stopped
