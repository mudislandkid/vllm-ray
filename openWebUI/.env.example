# Open WebUI Configuration

# vLLM Backend URL
# Use host.docker.internal for same machine, or actual IP for remote
VLLM_HOST=host.docker.internal
VLLM_PORT=8000

# Open WebUI Port
WEBUI_PORT=3000

# SearXNG Port (external)
SEARXNG_PORT=8888
